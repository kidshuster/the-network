kind: embed
title: "Moderation — {network_display_name}"
colour: dark_grey
description: |
  Manage your **`{network_key}`** subscription from **`{client_slug}-profile`**.
  Publish to **`{client_slug}-{network_key}-publish`** and follow **`{client_slug}-{network_key}-subscribe`** for webhooks.
  Use **Blacklist** to hide announcements from specific clients, or **Leave {network_key}** to unsubscribe.
