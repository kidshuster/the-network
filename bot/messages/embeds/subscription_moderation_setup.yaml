kind: embed
title: "Moderation — {network_display_name}"
colour: dark_grey
description: "{setup_description}"
fields:
  - name: Publish channel
    value: "{publish_mention} — **not connected**"
    inline: false
    when: "{needs_publish}"
  - name: Publish setup
    value: "Follow the instruction card in {publish_mention}."
    inline: false
    when: "{needs_publish}"
  - name: Subscribe channel
    value: "{subscribe_mention} — **not connected**"
    inline: false
    when: "{needs_subscribe}"
  - name: Subscribe setup
    value: "Follow the instruction card in {subscribe_mention}, then click **Subscribe connected** here or there."
    inline: false
    when: "{needs_subscribe}"
footer: "The Network • subscription setup"
