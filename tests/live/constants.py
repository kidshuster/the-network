from __future__ import annotations

# Intentionally distinct audit-log reasons per smoke workflow.
TEST_CLEANUP_REASON = "The Network test cleanup (auto-deleted)"
SMOKE_CLEANUP_REASON = "The Network smoke cleanup (auto-deleted)"
SERVER_INIT_PROBE_REASON = "The Network server-init live probe (auto-reverted)"
